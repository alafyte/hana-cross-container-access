VIEW "com.sap.training.cross.container::CUSTOMER_ORDERS" AS
SELECT 
  C.CUSTOMER_ID,
  C.NAME,
  O.ORDER_ID,
  O.AMOUNT
FROM "com.sap.training.cross.container::CUSTOMERS" C
JOIN "com.sap.training.cross.container::ORDERS" O
  ON C.CUSTOMER_ID = O.CUSTOMER_ID;